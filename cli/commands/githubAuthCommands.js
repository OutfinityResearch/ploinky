// Stub — file was referenced in commit but not included
export function handleGithubAuthCommand() {
  console.log("GitHub auth not yet configured. Please check the ploinky documentation.");
}
export default { handleGithubAuthCommand };
